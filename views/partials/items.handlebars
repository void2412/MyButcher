<div class="row itemRow" item-id="{{item_id}}">
	<div class="col-3 itemName">{{item_name}}</div>
	<div class="col itemQuantity text-end input-group">
		<input class="text-end form-control" type="number" id="quantityInput" min="0"></input>
		<div class="input-group-append"><span class="input-group-text">kg</span></div>
		
	</div>
	<div class="col text-center itemPrice">${{unit_price}}</div>
	<div class="col text-center itemDiscount">{{discount}}%</div>
	<div class="col text-center itemTaxRate">{{tax_rate}}%</div>
	<div class="col text-center itemTotal">$0</div>
</div>